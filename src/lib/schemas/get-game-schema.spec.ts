import { describe, it } from 'vitest';

describe('getGame', () => {
	it.todo('Should get a game');

	it.todo('Should throw an error if the provided id is not a string');

	it.todo('Should throw an error if the provided id is not a uuid v4');
});
